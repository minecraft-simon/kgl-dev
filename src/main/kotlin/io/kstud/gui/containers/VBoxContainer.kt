package io.kstud.gui.containers

import io.kstud.gui.widgets.UIElement

class VBoxContainer(
    parent: Container? = null,
    minWidth: Int = 0,
    minHeight: Int = 0
): Container(parent, minWidth, minHeight) {

}